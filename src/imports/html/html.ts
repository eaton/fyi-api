// Placeholder for an importer that takes an arbitrary folder full of
// HTML files and turns them into content. Options should include 
// css selectors for specific page elements, and the option to cache
// remote media files.